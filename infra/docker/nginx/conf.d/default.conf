server {
    listen 80;
    server_name vps-5085022-x.dattaweb.com;

    location /.well-known/acme-challenge/ {
        root /var/certbot;
        allow all;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name vps-5085022-x.dattaweb.com;

    ssl_certificate /etc/letsencrypt/live/vps-5085022-x.dattaweb.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vps-5085022-x.dattaweb.com/privkey.pem;

    # Proxy para el servicio
    location / {
        # Pasar la solicitud al contenedor de Ticketo
        proxy_pass http://ticketo:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
